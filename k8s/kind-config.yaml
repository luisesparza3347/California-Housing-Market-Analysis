kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    # NodePort 30080 -> host 8080, used by the FastAPI Service.
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
